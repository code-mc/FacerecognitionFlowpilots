<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="com.commonsware.cwac.cam2.playground.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:minHeight="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:title="@string/title_activity_main"/>

    <TextView
        android:id="@+id/main_select_picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:paddingLeft="24dp"
        android:paddingRight="24dp"
        android:paddingBottom="16dp"

        android:layout_centerInParent="true"

        android:background="@drawable/raised_button"

        android:text="@string/select_picture"
        android:textAppearance="@style/TextAppearance.AppCompat.Headline"
        android:textColor="#fff"
        android:gravity="center"
        android:clickable="true"/>

    <TextView
        android:id="@+id/main_sign_in"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_marginTop="16dp"
        android:paddingTop="8dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingBottom="16dp"

        android:layout_below="@id/main_select_picture"
        android:layout_alignLeft="@id/main_select_picture"
        android:layout_alignRight="@id/main_select_picture"

        android:background="@drawable/raised_button"

        android:text="@string/sign_in"
        android:textAppearance="@style/TextAppearance.AppCompat.Headline"
        android:textColor="#fff"
        android:gravity="center"
        android:clickable="true"/>

    <ImageView
        android:id="@+id/main_image_preview"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_alignParentBottom="true"
        android:scaleType="fitCenter"/>

</RelativeLayout>
